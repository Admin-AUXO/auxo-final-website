# Astro Configuration
# Base path for GitHub Pages deployment
# Use '/' for root deployment (custom domain or username.github.io)
# Use '/repo-name/' for subdirectory deployment (e.g., '/Final-Website/')
BASE_PATH=/

# Site URL (optional, used in GitHub Actions workflow)
# Defaults to https://auxodata.com if not set
SITE_URL=https://auxodata.com

# Note: BASE_URL is automatically set by Astro based on the 'base' config
# It's available via import.meta.env.BASE_URL in your code
