---
import { siteData } from "../data/config/site";
import Navigation from "../components/common/Navigation.astro";
import Footer from "../components/common/Footer.astro";
import ParticleBackground from "../components/effects/ParticleBackground.astro";
import ScrollProgressBar from "../components/ui/ScrollProgressBar.astro";
import CalendlyModal from "../components/widgets/CalendlyModal.astro";
import { createAssetUrl } from "../utils/url";
import "../styles/tokens.css";
import "../styles/base.css";
import "../styles/typography.css";
import "../styles/backgrounds.css";
import "../styles/utilities.css";
import "../styles/animations.css";
import "../styles/components/base-components.css";
import "../styles/components/buttons.css";
import "../styles/components/cards.css";
import "../styles/components/carousels.css";
import "../styles/components/accordions.css";
import "../styles/components/highlights.css";
import "../styles/navigation.css";
import "../styles/sections/shared-sections.css";
import "../styles/sections/hero.css";
import "aos/dist/aos.css";
import "notyf/notyf.min.css";

export interface Props {
  title?: string;
  description?: string;
  showParticles?: boolean;
}

const { title, description, showParticles = true } = Astro.props;
const pageTitle = title || siteData.name;
const pageDescription = description || siteData.description;
const ogImage = createAssetUrl("/og-image.svg");
---

<!doctype html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={pageDescription} />
    <meta name="theme-color" content={siteData.themeColor} />
    <meta name="color-scheme" content="light dark" />
    <title>{pageTitle}</title>

    <link rel="icon" type="image/svg+xml" href={createAssetUrl("/favicon.svg")} />
    <link rel="alternate icon" href={createAssetUrl("/favicon.svg")} />
    <link rel="apple-touch-icon" href={createAssetUrl("/apple-touch-icon.svg")} />
    <link rel="manifest" href={createAssetUrl("/manifest.json")} />
    <meta name="msapplication-TileColor" content={siteData.themeColor} />
    <meta name="msapplication-config" content={createAssetUrl("/browserconfig.xml")} />

    <meta property="og:type" content="website" />
    <meta property="og:url" content={siteData.url} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={pageDescription} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:site_name" content={siteData.name} />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:url" content={siteData.url} />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={pageDescription} />
    <meta name="twitter:image" content={ogImage} />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700;800&display=swap&subset=latin" rel="stylesheet" media="print" onload="this.media='all'" />
    <noscript><link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700;800&display=swap&subset=latin" rel="stylesheet" /></noscript>
    <style is:inline>
      @font-face{font-family:'Plus Jakarta Sans';font-display:swap}*{margin:0;padding:0;box-sizing:border-box}html{scroll-behavior:auto;-webkit-font-smoothing:antialiased;-moz-osx-font-smoothing:grayscale;overflow-x:hidden!important;overflow-y:hidden!important;max-width:100vw;max-height:100vh;scrollbar-width:none!important;-ms-overflow-style:none!important}html::-webkit-scrollbar{display:none!important;width:0!important;height:0!important;background:transparent!important}html::-webkit-scrollbar-track{display:none!important;background:transparent!important}html::-webkit-scrollbar-thumb{display:none!important;background:transparent!important}html.loaded{overflow-y:auto;max-height:none;scrollbar-width:none!important;-ms-overflow-style:none!important}body{font-family:'Plus Jakarta Sans',system-ui,-apple-system,sans-serif;color:#FFF;background-color:#000;line-height:1.6;overflow-x:hidden!important;max-width:100vw;overflow-y:auto;scrollbar-width:none!important;-ms-overflow-style:none!important}body::-webkit-scrollbar{display:none!important;width:0!important;height:0!important;background:transparent!important}body::-webkit-scrollbar-track{display:none!important;background:transparent!important}body::-webkit-scrollbar-thumb{display:none!important;background:transparent!important}body.loading{overflow:hidden;height:100vh}html,body{-webkit-overflow-scrolling:touch}#main-navigation{position:fixed;top:0;left:0;right:0;z-index:1000;backdrop-filter:blur(8px);-webkit-backdrop-filter:blur(8px)}.min-h-screen{min-height:100vh}.flex{display:flex}.flex-col{flex-direction:column}.relative{position:relative}.bg-primary{background-color:#000}.text-primary{color:#FFF}[data-lazy-load]{opacity:0;transition:opacity .3s ease-in}[data-lazy-load].lazy-loaded{opacity:1}@supports(view-transition-name:none){::view-transition-old(root),::view-transition-new(root){animation-duration:0.5s;animation-timing-function:cubic-bezier(0.4,0,0.2,1)}::view-transition-old(main-content),::view-transition-new(main-content){animation-duration:0.5s;animation-timing-function:cubic-bezier(0.34,1.56,0.64,1)}}
    </style>
    <link rel="preconnect" href="https://cdn.sanity.io" />
    <link rel="dns-prefetch" href="https://cdn.sanity.io" />
    <link rel="preconnect" href="https://assets.calendly.com" crossorigin />
    <link rel="dns-prefetch" href="https://assets.calendly.com" />
    <link rel="preconnect" href="https://calendly.com" crossorigin />
    <link rel="dns-prefetch" href="https://calendly.com" />

    <link rel="canonical" href={siteData.url} />
    <link rel="dns-prefetch" href="https://fonts.googleapis.com" />
    <link rel="dns-prefetch" href="https://fonts.gstatic.com" />
    <script>
      (function initViewTransitions() {
        if (typeof document === 'undefined' || !('startViewTransition' in document)) return;
        const setViewTransitionName = () => {
          const main = document.querySelector('main');
          if (main) {
            main.style.viewTransitionName = 'main-content';
          }
        };
        setViewTransitionName();
        document.addEventListener('astro:before-preparation', setViewTransitionName);
        document.addEventListener('astro:after-swap', setViewTransitionName);
      })();
    </script>
    <script is:inline define:vars={{ baseUrl: import.meta.env.BASE_URL }}>
      (function() {
        if (typeof document === 'undefined' || !document.documentElement || !document.body) return;
        
        const root = document.documentElement;
        const body = document.body;
        
        function applyThemeImmediate() {
          if (!root) return;
          const stored = localStorage.getItem('theme');
          const prefersLight = window.matchMedia('(prefers-color-scheme: light)').matches;
          const theme = stored || (prefersLight ? 'light' : 'dark');
          root.classList.remove('dark', 'light');
          root.classList.add(theme);
        }
        
        function enableScroll() {
          if (!root || !body) return;
          root.classList.remove('loading');
          root.classList.add('loaded');
          body.classList.remove('loading');
        }
        
        applyThemeImmediate();
        if (root && body) {
          root.classList.add('loading');
          body.classList.add('loading');
        }
        
        if (document.readyState === 'loading') {
          document.addEventListener('DOMContentLoaded', enableScroll);
        } else {
          setTimeout(enableScroll, 0);
        }
        
        window.addEventListener('load', enableScroll);
        setTimeout(enableScroll, 100);
      })();
      
      const blockedPatterns = ['fbevents', 'facebook.net', 'connect.facebook.net', 'ERR_BLOCKED_BY_CLIENT', 'segment.io', 'analytics.js'];
      
      window.addEventListener('error', function(e) {
        const target = e.target || e.currentTarget;
        const message = e.message || '';
        const filename = e.filename || '';
        const url = (target && target.src) || '';
        
        if (blockedPatterns.some(pattern => message.includes(pattern) || filename.includes(pattern) || url.includes(pattern))) {
          e.preventDefault();
          e.stopPropagation();
          return false;
        }
      }, true);
      
      window.addEventListener('unhandledrejection', function(e) {
        const reason = String(e.reason || '');
        if (reason.includes('facebook.net') || reason.includes('fbevents') || reason.includes('ERR_BLOCKED_BY_CLIENT')) {
          e.preventDefault();
        }
      }, true);
      
      if ('serviceWorker' in navigator) {
        window.addEventListener('load', () => {
          const base = baseUrl === '/' ? '' : baseUrl.replace(/\/$/, '');
          navigator.serviceWorker.register(`${base}/sw.js`)
            .catch(() => {});
        });
      }
    </script>
    <script>
      import { setupServiceWorkerClearing } from "../scripts/utils/serviceWorker";
      import { setupCalendly } from "../scripts/widgets/calendly";
      setupServiceWorkerClearing();
      setupCalendly();
    </script>
  </head>
  <body class="min-h-screen flex flex-col relative bg-primary transition-colors duration-normal">
    <ScrollProgressBar />
    {showParticles && <ParticleBackground />}
    <Navigation />
    <main id="main-content" class="flex-1 relative z-content">
      <slot />
    </main>
    <Footer />

    <script>
      import { initSmoothScroll } from "../scripts/smoothScroll";
      import { initScrollAnimations } from "../scripts/scrollAnimations";
      
      function initLazyLoading() {
        const lazyElements = document.querySelectorAll('[data-lazy-load]');
        if (lazyElements.length === 0) return;

        lazyElements.forEach((element) => {
          const observer = new IntersectionObserver((entries) => {
            for (const entry of entries) {
              if (entry.isIntersecting) {
                const el = entry.target as HTMLElement;
                el.classList.add('lazy-loaded');
                el.removeAttribute('data-lazy-load');
                observer.unobserve(el);
                observer.disconnect();
              }
            }
          }, { rootMargin: '50px', threshold: 0.01 });
          
          observer.observe(element);
        });
      }
      
      const initScripts = () => {
        if ('requestIdleCallback' in window) {
          requestIdleCallback(() => {
            initSmoothScroll();
            initScrollAnimations();
            initLazyLoading();
          }, { timeout: 2000 });
        } else {
          setTimeout(() => {
            initSmoothScroll();
            initScrollAnimations();
            initLazyLoading();
          }, 1);
        }
      };
      
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initScripts);
      } else {
        initScripts();
      }
    </script>
  </body>
</html>
