---
import { Icon } from "astro-icon/components";
import SectionContainer from "@/components/layouts/SectionContainer.astro";
import SectionHeader from "@/components/ui/SectionHeader.astro";
import HighlightText from "@/components/ui/HighlightText.astro";
import BackgroundDecorations from "@/components/ui/BackgroundDecorations.astro";
import Carousel from "@/components/ui/Carousel.astro";
import type { ServiceDetail } from "@/data/content/services";

interface Props {
  service: ServiceDetail;
}

type BenefitFeature = {
  title: string;
  description: string;
  icon: string;
};

const { service } = Astro.props;
---

<SectionContainer
  id="service-benefits"
  padding="lg"
  background="default"
  class="relative service-benefits-section section-divider overflow-hidden"
>
  <BackgroundDecorations gradients={["hero"]} />

  <div class="grid-container relative z-10">
    <div class="col-span-full max-w-[1400px] mx-auto w-full">
      <div data-reveal="fade-down" data-reveal-duration="600" data-reveal-easing="ease-out-cubic">
        <SectionHeader
          title={service.benefits.title}
          subtitle={service.benefits.description}
          subtitleHighlight={service.benefits.descriptionHighlight}
        />
      </div>

      <div class="hidden lg:grid lg:grid-cols-4 gap-6 lg:gap-8">
        {service.benefits.features.map((feature: BenefitFeature, index: number) => (
          <div
            class="group"
            data-reveal="zoom-in"
            data-reveal-delay={index * 50}
            data-reveal-duration="500"
            data-reveal-easing="ease-out-cubic"
          >
            <div class="h-full bg-theme-card border border-accent-green/10 rounded-2xl p-6 lg:p-8 relative overflow-hidden transition-all duration-500 hover:border-accent-green/40 hover:-translate-y-1">
              <div class="absolute top-0 left-0 right-0 h-1 bg-gradient-to-r from-accent-green/0 via-accent-green/30 to-accent-green/0 group-hover:via-accent-green/60 transition-all duration-500" />

              <div class="relative z-10">
                <div class="relative inline-flex mb-6">
                  <div class="relative w-12 h-12 bg-accent-green/10 rounded-xl flex items-center justify-center border border-accent-green/20 group-hover:bg-accent-green/20 group-hover:border-accent-green/40 transition-all duration-500">
                    <Icon name={feature.icon} class="w-6 h-6 text-accent-green" />
                  </div>
                </div>

                <h3 class="text-h4 font-bold text-theme-primary leading-tight mb-3 lg:mb-4 group-hover:text-accent-green transition-colors duration-500 font-display">
                  {feature.title}
                </h3>
                <p class="text-body-lg text-theme-secondary leading-relaxed font-body">
                  {feature.description}
                </p>
              </div>

              <div class="absolute bottom-0 left-0 right-0 h-0.5 bg-accent-green/30 opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
            </div>
          </div>
        ))}
      </div>

      <div class="lg:hidden">
        <Carousel
          containerId="service-benefits-carousel-container"
          trackId="service-benefits-carousel-track"
          controlSelector=".service-benefits-carousel-control"
          items={service.benefits.features}
          wrapperClass="service-benefits-carousel-wrapper"
          dotsClass="service-benefits-carousel-dots"
          dotClass="service-benefits-carousel-dot"
          breakpoint={1024}
          activateOnDesktop={false}
        >
          {service.benefits.features.map((feature: BenefitFeature, index: number) => (
            <div class="carousel-slide embla__slide flex-shrink-0 service-benefits-carousel-slide" data-index={index}>
              <div class="h-full bg-theme-card border border-accent-green/10 rounded-2xl relative overflow-hidden transition-all duration-500 hover:border-accent-green/40 flex flex-col min-h-[var(--card-min-height-sm)] sm:min-h-[var(--card-min-height-sm)]">
                <div class="absolute top-0 left-0 right-0 h-1 bg-gradient-to-r from-accent-green/0 via-accent-green/30 to-accent-green/0 hover:via-accent-green/60 transition-all duration-500" />

                <div class="relative z-10 flex flex-col h-full p-4 sm:p-5 lg:p-6">
                  <div class="flex items-start justify-between mb-4 sm:mb-5">
                    <h3 class="text-base sm:text-lg lg:text-xl font-bold text-theme-primary leading-tight font-display flex-1 pr-3">
                      {feature.title}
                    </h3>
                    <div class="w-10 h-10 sm:w-11 sm:h-11 bg-accent-green/10 rounded-xl flex items-center justify-center border border-accent-green/20 flex-shrink-0">
                      <Icon name={feature.icon} class="w-5 h-5 sm:w-5.5 sm:h-5.5 text-accent-green" />
                    </div>
                  </div>

                  <p class="text-sm sm:text-base text-theme-secondary leading-relaxed font-body flex-grow">
                    {feature.description}
                  </p>
                </div>

                <div class="absolute bottom-0 left-0 right-0 h-0.5 bg-accent-green/30" />
              </div>
            </div>
          ))}
        </Carousel>
      </div>
    </div>
  </div>
</SectionContainer>

